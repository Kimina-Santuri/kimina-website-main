{
  "name": "kimina-portfolio",
  "private": true,
  "version": "1.0.0",
  "description": "Static portfolio and Git-backed editorial content for Kimina.",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "cms": "node cms/server.js",
    "check": "node --check landing.js && node --check bookings.js && node --check stellar-player.js && node tools/validate-site.js _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "3.1.6",
    "markdown-it": "14.3.0",
    "yaml": "2.9.0"
  }
}
